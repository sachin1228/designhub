I need you to fix ONE remaining UX/performance issue in the current PR.

IMPORTANT:
Read the CURRENT CommunityChat.tsx and CommunitiesPanel.tsx first.

Do not rewrite the unread-divider architecture again.
Do not add artificial bottom spacers.
Do not change the visual design of the divider.
Do not remove the current realtime behavior.

The unread divider is mostly working now.

THE BUG:

When I open a community with 10+ unread messages, I see this:

1. Chat opens.
2. Message viewport initially appears at the TOP / wrong position.
3. I can see old messages for roughly 1–2 seconds.
4. Then suddenly the viewport jumps down to:
   "X unread messages"

This visible delayed jump is bad UX.

The FIRST visible frame of the chat should already be positioned correctly.

==================================================
I CHECKED THE CURRENT CODE — HERE IS THE PROBLEM
==================================================

CommunityChat currently has:

initialMessagesReady

and the unread snapshot waits for:

if (!initialMessagesReady) return;

But initialMessagesReady is only set after:

await msgPromise

finishes.

Therefore the current critical path is roughly:

open community
    ↓
cached messages render
    ↓
browser shows them at scrollTop=0        ❌
    ↓
wait for incremental message HTTP request
    ↓
msgPromise resolves
    ↓
setInitialMessagesReady(true)
    ↓
React render
    ↓
create unread snapshot
    ↓
setSnapshotReady(true)
    ↓
another React render
    ↓
initial scroll runs
    ↓
finally user reaches unread divider

That is exactly the visible 1–2 second jump I am seeing.

DO NOT make the incremental network fetch part of the initial
viewport positioning when the cached data is already sufficient.


==================================================
ALSO REMOVE THE 800ms VISUAL WAIT
==================================================

The current code also has a fallback around lastReadAt that waits
approximately 800ms for lastReadAtOnOpen / PATCH.

That network/fallback wait must NOT cause the user to see the
conversation at scrollTop=0.

PATCH /read is persistence.

It should NOT control when the chat is allowed to visually settle.


==================================================
GOAL
==================================================

For normal sidebar navigation with warm cache:

CLICK COMMUNITY
        ↓
use cached messages immediately
        ↓
use opening lastReadAt snapshot immediately
        ↓
calculate unread boundary
        ↓
render divider
        ↓
position viewport BEFORE PAINT
        ↓
user sees correct unread position immediately

Meanwhile:

incremental fetch
PATCH /read
metadata refresh

can continue in the background.


==================================================
STEP 1 — CAPTURE lastReadAt BEFORE MARKING READ
==================================================

Inspect CommunitiesPanel.tsx.

Current handleNavigate roughly does:

markReadOnServer(id)

clear message_count

router.push(...)


We already have lastReadAtOnOpen infrastructure.

Make absolutely sure the OLD last_read_at is put into:

lastReadAtOnOpen

BEFORE:

- message_count becomes 0
- markReadOnServer updates last_read_at
- navigation happens

The opening snapshot must be synchronous whenever the sidebar already
has the necessary last_read_at value.


Correct conceptual sequence:

function handleNavigate(id) {

    // FIRST:
    capture OLD unread/read boundary
    lastReadAtOnOpen.set(id, oldLastReadAt);

    // THEN:
    clear local badge immediately

    // THEN:
    router.push(...);

    // mark-read persistence can happen without blocking UI
    void markReadOnServer(id);
}


Do not make CommunityChat wait for PATCH just to discover what
last_read_at USED TO BE.


==================================================
STEP 2 — DO NOT WAIT FOR initialMessagesReady IF CACHE IS USABLE
==================================================

This is the main performance fix.

When CommunityChat opens, it already reads:

const cachedMsgs = msgCache.get(communityId);

If:

- cachedMsgs exist
- lastReadAtOnOpen has a resolved value

then calculate the unread snapshot IMMEDIATELY from those cached
messages.

Do this during the client initialization/layout phase.

Do NOT wait for:

await msgPromise

just to create the divider.


Conceptually:

const cachedMsgs = msgCache.get(communityId);
const openingLastReadAt = lastReadAtOnOpen.get(communityId);

if (
    cachedMsgs?.length &&
    openingLastReadAt !== undefined
) {

    const unreadMessages = cachedMsgs.filter(
        m =>
            !m.id.startsWith("temp-") &&
            m.user_id !== currentUserId &&
            (
                openingLastReadAt === null ||
                new Date(m.created_at).getTime() >
                new Date(openingLastReadAt).getTime()
            )
    );

    unreadAtOpenRef.current = {
        firstMsgId: unreadMessages[0]?.id ?? null,
        count: unreadMessages.length
    };

    setSnapshotReady(true);
}


The incremental fetch should STILL run afterward.

We are not removing catch-up.

We are only removing it from the FIRST-PAINT critical path.


==================================================
IMPORTANT SAFETY CHECK
==================================================

Do NOT blindly freeze a count of 0 from cache if we know the sidebar
had unread messages.

If the sidebar/open snapshot says:

message_count > 0

but cached messages produce:

count === 0

then the cache is clearly insufficient/stale.

In THAT exceptional case:

do not finalize a zero unread snapshot.

Wait for the incremental fetch.

But:

DO NOT SHOW THE CHAT AT scrollTop=0 WHILE WAITING.

Keep the message viewport visually hidden/contained until initial
positioning can be resolved, while keeping:

- chat header visible
- members panel visible
- composer visible

Then reveal the message content once positioned.


==================================================
STEP 3 — USE useLayoutEffect FOR INITIAL POSITIONING
==================================================

Initial unread positioning should NOT use a normal useEffect if that
allows the browser to paint scrollTop=0 first.

Use the existing isomorphic layout-effect pattern for the INITIAL
position only.

The sequence should be:

DOM renders messages + divider

useLayoutEffect:
    calculate overflow
    set correct scrollTop

browser paints

Therefore the user never sees the incorrect top position.


Do NOT use smooth scrolling for initial positioning.

Initial positioning must be instant.

No animation.

No visible jump.


==================================================
STEP 4 — SHORT CHAT BEHAVIOR MUST REMAIN CORRECT
==================================================

We already established this UX rule:

If total message content is SHORTER than the available chat viewport:

messages must sit at the BOTTOM above the composer.

Example:

┌──────────────────────────────┐
│                              │
│                              │
│                              │
│                              │
│ ----- 1 unread message ----- │
│ message                      │
├──────────────────────────────┤
│ composer                     │
└──────────────────────────────┘


DO NOT force the divider to the top/center for short chats.

DO NOT add fake spacer content.


The message area should use a safe inner wrapper such as:

scroll container:
    overflow-y-auto

inner message wrapper:
    min-h-full
    flex
    flex-col
    justify-end


Be careful that long overflowing chats remain fully scrollable.


==================================================
STEP 5 — INITIAL SCROLL RULE
==================================================

After layout:

const container = scrollContainerRef.current;

Determine whether the message timeline actually overflows.

If it DOES NOT overflow:

    do nothing.

    The inner wrapper's bottom alignment is already the correct UX.

    initialScrollDoneRef.current = true;


If it DOES overflow AND there is an unread divider:

    position the unread divider near the top portion of the viewport.

    Prefer direct scrollTop calculation instead of smooth
    scrollIntoView.

Example:

const dividerRect = unreadDividerRef.current.getBoundingClientRect();
const containerRect = container.getBoundingClientRect();

const dividerTop =
    dividerRect.top -
    containerRect.top +
    container.scrollTop;

container.scrollTop =
    Math.max(0, dividerTop - 80);


Approximately 60–100px of previous context above the divider is fine.


If it DOES overflow and there is NO unread boundary:

    go to bottom instantly.


==================================================
STEP 6 — DO NOT SHOW WRONG POSITION WHILE FALLBACK LOADS
==================================================

This is extremely important.

If we genuinely cannot determine the unread boundary immediately
because:

- cache is incomplete
- opening lastReadAt is unavailable

then DO NOT render the message timeline visibly at scrollTop=0 and
later jump it.

That is the exact bug I'm asking you to eliminate.

Use a temporary initialization flag such as:

initialPositionResolved

While false:

message timeline can use:

visibility: hidden

NOT:

display: none

because the DOM must exist so its height/scroll position can be
measured.

Header/composer/members should remain visible.


Then:

1. messages render invisibly
2. unread state resolves
3. layout effect positions container
4. set initialPositionResolved
5. reveal messages

The user should NEVER see:

old messages at top
    ↓
wait
    ↓
jump to unread

A very short hidden message area is preferable to showing a visibly
wrong scroll position.


==================================================
STEP 7 — DO NOT BREAK REALTIME
==================================================

This optimization is ONLY for INITIAL OPEN.

After:

initialScrollDoneRef.current === true

keep the existing realtime behavior:

incoming message while near bottom:
    follow bottom

incoming message while reading history:
    do not move viewport
    show ChevronDown button


Realtime messages must NOT change:

unreadAtOpenRef
firstUnreadMsgId
unreadDisplayCount


==================================================
STEP 8 — DON'T REINTRODUCE DIVIDER DISMISSAL
==================================================

Do NOT add:

dividerDismissed

Do NOT add:

IntersectionObserver to remove divider

Do NOT remove divider after timeout.

The unread divider remains attached to firstUnreadMsgId for the
current mounted chat session.


==================================================
EXPECTED FAST PATH
==================================================

This is the important architecture.

NORMAL SIDEBAR NAVIGATION:

sidebar already loaded
        ↓
user clicks community
        ↓
capture:
    unread count
    old lastReadAt
        ↓
router navigation
        ↓
CommunityChat reads warm msgCache
        ↓
calculate first unread synchronously
        ↓
render
        ↓
useLayoutEffect positions scroll
        ↓
FIRST PAINT IS CORRECT
        ↓
background incremental fetch continues


No 1–2 second dependency.


==================================================
SLOW/FALLBACK PATH
==================================================

If cache genuinely does NOT contain enough information:

open community
        ↓
keep message timeline visibility:hidden
        ↓
fetch/catch-up
        ↓
resolve old lastReadAt
        ↓
calculate snapshot
        ↓
render divider
        ↓
layout effect sets scrollTop
        ↓
reveal message timeline


Again:

NEVER show scrollTop=0 and then jump.


==================================================
ACCEPTANCE TEST A — 10+ UNREAD, WARM CACHE
==================================================

Have 15 unread messages.

Hover/click community so cache is warm.

Click it.

EXPECTED:

The FIRST visible frame already shows approximately:

previous message

------- 15 unread messages -------

first unread
second unread
third unread
...


There must be NO:

top of conversation
→ 1–2 second wait
→ jump


==================================================
ACCEPTANCE TEST B — ONE UNREAD / SHORT CHAT
==================================================

Conversation is short enough to fit in viewport.

One unread message.

Expected:

empty space
empty space
empty space

------- 1 unread message -------
message

composer


The whole message timeline sits at bottom.

Do NOT force divider upward.


==================================================
ACCEPTANCE TEST C — NO UNREAD
==================================================

Open a previously read conversation.

Short conversation:
    messages bottom-aligned.

Long conversation:
    start at latest/bottom.

No visible top → bottom jump.


==================================================
ACCEPTANCE TEST D — COLD CACHE
==================================================

Open a community whose messages aren't cached.

If network takes 1 second:

I must NOT see old/top-positioned messages for that second.

Keep message timeline hidden/loading until enough data exists,
position it BEFORE revealing it.

Header and composer can remain visible.


==================================================
VERY IMPORTANT
==================================================

Do not "fix" this by reducing the 800ms timeout to 200ms.

Do not add setTimeout hacks.

Do not use smooth scrolling to hide the jump.

Do not remove incremental fetch.

The real fix is:

FAST PATH:
cached data + opening unread snapshot → position before paint.

FALLBACK:
if data isn't ready → don't reveal an incorrect scroll position.

And preserve:

SHORT CHAT = bottom aligned.
LONG UNREAD CHAT = open at unread boundary.


After implementing, run the TypeScript/build checks and tell me:

1. Which files changed
2. How normal sidebar navigation now gets lastReadAt immediately
3. Whether msgPromise is still blocking initial positioning anywhere
4. How the wrong scrollTop=0 first paint is prevented
5. How short chats are bottom-aligned
6. Results of build/typecheck

Do not change unrelated community/realtime code.