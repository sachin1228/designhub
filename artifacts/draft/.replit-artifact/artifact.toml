kind = "web"
previewPath = "/"
title = "draft"
version = "1.0.0"
id = "artifacts/draft"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18726

[services.development]
run = "pnpm --filter @workspace/draft run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/draft", "run", "build" ]
serve = "static"
publicDir = "artifacts/draft/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18726"
BASE_PATH = "/"
